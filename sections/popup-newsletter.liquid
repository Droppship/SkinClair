{%- if section.settings.enable -%}
  <div
    class="popup"
    id="NewsletterPopup"
    role="dialog"
    aria-modal="true"
    aria-labelledby="NewsletterPopupTitle"
    data-delay="{{ section.settings.delay }}"
    data-frequency="{{ section.settings.frequency }}"
  >
    <div class="popup__backdrop" data-popup-close></div>
    <div class="popup__card">
      <button class="popup__close" data-popup-close aria-label="{{ 'accessibility.close' | t }}">
        {% render 'icon', name: 'close', size: 18 %}
      </button>
      <div class="popup__media">
        {%- if section.settings.image != blank -%}
          <img src="{{ section.settings.image | image_url: width: 800 }}" alt="" width="400" height="500" loading="lazy">
        {%- else -%}
          {{ 'lifestyle-1' | placeholder_svg_tag: 'placeholder-svg' }}
        {%- endif -%}
      </div>
      <div class="popup__content">
        <span class="eyebrow">{{ section.settings.eyebrow }}</span>
        <h2 id="NewsletterPopupTitle">{{ section.settings.heading }}</h2>
        <p>{{ section.settings.text }}</p>
        {%- form 'customer', id: 'PopupNewsletter' -%}
          <input type="hidden" name="contact[tags]" value="newsletter,popup">
          {%- if form.posted_successfully? -%}
            <p class="form-message form-message--success" data-newsletter-success hidden>{{ 'general.newsletter.success' | t }}</p>
          {%- else -%}
            <div class="newsletter__form">
              <input
                type="email"
                name="contact[email]"
                placeholder="{{ 'general.newsletter.placeholder' | t }}"
                aria-label="{{ 'general.newsletter.label' | t }}"
                autocomplete="email"
                required
              >
              <button type="submit" class="btn">{{ section.settings.button_label }}</button>
            </div>
          {%- endif -%}
        {%- endform -%}
        <button class="popup__dismiss" data-popup-close>{{ section.settings.dismiss_label }}</button>
      </div>
    </div>
  </div>
{%- endif -%}

{% schema %}
{
  "name": "Email pop-up",
  "settings": [
    {
      "type": "checkbox",
      "id": "enable",
      "label": "Enable pop-up",
      "default": false
    },
    {
      "type": "range",
      "id": "delay",
      "label": "Appearance delay",
      "min": 2,
      "max": 30,
      "step": 1,
      "unit": "s",
      "default": 6
    },
    {
      "type": "range",
      "id": "frequency",
      "label": "Don't show again for",
      "min": 1,
      "max": 30,
      "step": 1,
      "unit": "d",
      "default": 7
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Image"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Welcome offer"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "10% off your first order"
    },
    {
      "type": "text",
      "id": "text",
      "label": "Text",
      "default": "Sign up and get your exclusive code, our beauty rituals, and private sales."
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button",
      "default": "Get my 10% off"
    },
    {
      "type": "text",
      "id": "dismiss_label",
      "label": "Dismiss link",
      "default": "No thanks, I'll pay full price"
    }
  ]
}
{% endschema %}
